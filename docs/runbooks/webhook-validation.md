# Webhook Validation

## M1 Requirement

Every webhook must be validated before processing.

## Current Scaffold

The validator classes exist as placeholders:

- `VapiWebhookValidator`
- `TwilioSignatureValidator`

The concrete Vapi and Twilio verification algorithms are added with the first webhook implementation slice.
